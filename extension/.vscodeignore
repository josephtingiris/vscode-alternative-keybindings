# Ignore local vsix artifacts
*.vsix

# Exclude development docs from packaged VSIX
DEVELOPMENT.md

# Keep asset files out (if present)
assets/

# Keep dist files out (if present)
dist/

# Keep node_modules out (if present)
node_modules/
