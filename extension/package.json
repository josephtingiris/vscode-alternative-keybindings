{
  "name": "alt-key",
  "displayName": "AlterNative Keybindings",
  "publisher": "josephtingiris",
  "version": "0.0.4",
  "repository": {
    "type": "git",
    "url": "https://github.com/josephtingiris/vscode-alternative-keybindings"
  },
  "icon": "vscode-alternative-keybindings.png",
  "license": "MIT",
  "engines": {
    "vscode": "^1.60.0"
  },
  "description": "Minimal extension that registers configuration under the altKey.* namespace for use in when-clauses and context debugging.",
  "categories": ["Other"],
  "contributes": {
    "configuration": {
      "title": "AlterNative Keybindings Settings",
      "properties": {
        "altKey.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable AlterNative altKey behaviors. Access in when-clauses as config.altKey.enabled."
        },
        "altKey.orientation": {
          "type": "string",
          "enum": ["default", "left", "right"],
          "default": "default",
          "description": "Preferred navigation orientation ('default' uses VS Code defaults). Use in when-clauses as config.altKey.orientation == 'left'."
        },
        "altKey.vim": {
          "type": "boolean",
          "default": true,
          "description": "Bind VIM key navigation (h, j, k, l) for UI component navigation. Use config.altKey.vim in when-clauses."
        },
        "altKey.wrap": {
          "type": "boolean",
          "default": true,
          "description": "When navigating across UI components, wrap around at screen edges. Use config.altKey.wrap in when-clauses."
        }
      }
    }
  }
}
