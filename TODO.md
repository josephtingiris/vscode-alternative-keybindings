# TODO

- add a visual indicator to status bar when extension is enabled/disabled (maybe color it red when disabled?)
- add a visusal indicator to the status bar when the extension 'stopped' the keybinding (i.e. `"command": "noop"`); this may require a new function (other than noop)